extends layout

block content
    h1 #{title}
    ul.list-group
        li.list-group-item
            a(href='/users/mailgun') Mailgun
        each article, i in articles
            li.list-group-item
                a(href='/articles/'+article._id) #{article.title}